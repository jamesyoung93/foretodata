---
import type { CollectionEntry } from 'astro:content';
import { sitePath } from '../utils/paths';

interface Props {
  posts: CollectionEntry<'blog'>[];
  heading?: string;
  description?: string;
}

const {
  posts,
  heading = 'Earlier technical notes',
  description = 'Selected analyses in forecasting, sequence alignment, and observational classification.',
} = Astro.props;
const formatDate = (date: Date) => date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric', timeZone: 'UTC' });
---

<section class="program-section">
  <div class="program-heading">
    <h2>{heading}</h2>
    <p>{description}</p>
  </div>
  <div class="archive-list">
    {posts.map((post) => (
      <a class="archive-item" href={sitePath(`posts/${post.slug}`)}>
        <span class="archive-meta">{formatDate(post.data.date)}</span>
        <div>
          <h3>{post.data.title}</h3>
          <p>{post.data.description}</p>
        </div>
        <span class="archive-arrow" aria-hidden="true">→</span>
      </a>
    ))}
  </div>
</section>
